# 填报表单抽屉验证记录

本切片在当前未提交工作树验证，原有改动保留，未提交、推送或替换本机 8081 服务。

## 已验证

- 后端 Java 21 最终组合共 92 项：91 通过、1 跳过（需专门启用的浏览器夹具）。其中 Integration 17、Engine 15、Copies 6、PresenceRegistry 1、Knowledge 53；失败及错误均 0。增加无配置表单和第二用户场景后再次通过。
- 本体 BusinessKnowledgeModelTest：53 项通过；新语义保持 specified/internal。
- 前端 TypeScript/Vite 构建通过；来源筛选、工序抽屉、快捷面板共 18 项测试通过；编辑心跳 3 项通过。
- 隔离浏览器使用实际 ProductionExecutionPage 和 H2 测试服务 18081，经临时本机 13000 代理加载 3000 源码，没有请求开发数据库。
- 隔离批次 B01 开工后，“作业发起 1”显示实际到达 FORM 节点的表单，初始仅查看在线人数为 0。
- 自定义分类可以进入模板选择，初始未选择必填规则时添加按钮禁用。显式选择模板版本和必填后挂载成功，生产填报数量由 1 变为 2，首份可以编辑。
- 编辑良品数量后人数显示 1；点击人数展示“测试操作员，第 1 份”，未触发表单切换。
- 工序切换弹出未保存保护，明确放弃后切换到没有表单的“检验”工序。左侧“选择或新增表单”入口可打开三分类抽屉，修复 QF-FORM-SELECTOR-001。
- 抽屉视觉检查：左边直角、约 40px 标题、420px 宽度、统一字号细分隔线、轻选中底色。

## 头像展示增量（2026-09-15）

- 原人数文字替换为 24px 重叠头像，最多 4 人，超出显示省略圆点；悬浮显示“当前 N 人填写中”，0 人及尚未获取人数时不渲染入口。点击沿用人员及份序号列表。
- 在线响应附加可选 avatarUrl，复用账号头像的 public-preview 地址；未设置或加载失败使用姓名首字。人数去重、编辑权限、会话过期、生产状态与持久化规则均未改变，本体门禁对此增量不适用。
- 头像与来源筛选 4 项前端测试通过；TypeScript/Vite 构建通过（/tmp/execution-avatars-build.log）。Java 21 Presence 集成测试与 Registry 测试共 2 项通过，覆盖账号头像、无头像用户、跨份去重和只读排除（/tmp/execution-avatars-backend.log）。
- 本增量未做浏览器视觉验收，未替换本机 8081 服务；实际照片需要后台加载新增头像投影，旧服务仍兼容姓名头像。
- 独立质量复核 avatar_display_quality 对本次 L1 头像增量返回 passed，无阻断发现；独立重跑前端 4 项测试通过。该结果不解除完整表单选择器的版本准入口径未决问题。

## 待确认与边界

- 源码核对发现模板模块 RdoVersionStatusResolver 以 effectiveFrom/effectiveTo 判断可引用状态，并明确 persisted lifecycle flags must not participate；旧 status 列存在但新版本默认 DRAFT，界面没有发布动作。
- 已向用户询问“已发布”是否等价于现有当前有效版本。当前严格 PUBLISHED 过滤仅验证了隔离测试数据，不能宣称真实模板新增列表已可用。收到决定后统一候选查询、挂载校验、界面文案与知识规则。
- 在线人数使用当前单服务内存心跳，不支持多后端实例聚合；异常断开最长 45 秒过期，不代表文档内容实时合并。
- 独立质量首轮指出空表单入口问题，已修复并完成浏览器验证；未参与该切片实现和初审的独立质量上下文已复核源码和证据，入口问题解决。工具的新建代理达到线程上限，复核使用既有独立上下文，不冒称创建了新代理。
- 本体门禁最终为 blocked-by-question，质量仍受同一业务问题阻塞；结构与测试通过不等同于完整交付通过。
