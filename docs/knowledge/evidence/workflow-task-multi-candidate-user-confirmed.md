# 运行时多候选工作流任务用户确认

日期：2026-09-04

本记录是用户确认的运行时任务语义证据，不代表当前工作流运行时已经实现。

- 每个流程节点创建一个逻辑 `workflow_task`；多候选人写入候选快照，不为每个候选人创建独立逻辑任务。
- 任一候选人完成审批即可完成该逻辑任务；同任务的其他候选快照中的候选用户同步置为 `CLOSED`。
- 只有创建时解析出的候选人可以完成任务，非候选人不得完成。
- 创建任务时保存主体解析结果快照及每个候选人的主体来源，用于审计；任务创建后组织或角色关系变化不影响该任务的候选集合。
