# DHR 汇总归档顺序确认

知识基线：`0.3.24`；确认日期：2026-09-24；证据等级：`user-confirmed`。

本次交接的用户确认：“拖到任意位置”指可选候选表单实例能插在同级目录或表单之间，保存、重开和提交冻结后保持顺序；放入后可移出本次汇总，但不删除源表单；同模板多实例分别作为候选和归档关联。DEC-0063 已确认的基础目录开工冻结、多级覆盖目录、实例级关联、可选候选不纳入和冻结版本不可变继续适用。

`beforeNodeKey`、`displayOrder`、静态节点相对顺序、锁图标展示及历史无排序版本的处理属于交接中的实现推断或展示决定，不作为新增的用户确认业务规则。本记录不证明代码测试或发布验证通过。
