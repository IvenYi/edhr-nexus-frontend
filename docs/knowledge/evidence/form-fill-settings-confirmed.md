# 作业与工序表单填报设置

核对日期：2026-09-22。知识基线：0.3.24。范围：DEC-PACKAGE-20260922-FORM-FILL-MODES，L2。

## user-confirmed

本任务用户明确确认：作业表单节点、工序绑定表单都显式选择“直接填报”或“按流程填报”；按流程填报沿用既有流程配置；直接填报配置权限组、字段例外、按钮和签署；两处共用组件，保存、发布快照和运行都须闭环。工序入口覆盖产品和产品簇的共享制程版本配置。当前功能切片没有业务未决问题，不代表知识库其他领域的未决问题已经关闭。

这是验收目标，不是已实现或已验证声明。按流程模式继续使用已发布表单流程版本及其节点配置；直接模式不要求另建审批流程，也不能解释为跳过已配置的权限或签署校验。

同一切片最新用户指令（user-confirmed）：当前 MVP 不需要兼容旧数据，旧数据可重造；取消历史数据回填、新旧数据双轨兼容及旧数据自动迁移方案。新执行快照不可变、权限与签署校验仍须保留。该指令撤销此前兼容性推断，不是新的实现证据；此前能力尚未独立质量验证，继续保持 specified/internal。本次仅登记产品决策，不执行数据删除或重建。

## secondary-reference

- `output/form-fill-settings-decision-package.json` 是交接索引，不作为唯一业务证据，也不作为发布资产引用。其 confirmed 只展开显式模式规则，本文件以本任务用户的完整确认补足其摘要范围。
- 本次续读时交接包仍保留 `inference.legacy-fallback`、compatibilityAndMigration 中的“历史缺失默认保持旧行为”、旧实例兼容说明、testImpacts 的“历史兼容”及 `scenario.legacy-fill`。这些兼容内容已由最新 user-confirmed 指令撤销，不再作为当前实现或验收依据；本体角色不修改 output 交接文件，由主智能体同步。
- DEC-0035 中的权限组、主体默认权限和字段例外、按钮事件规则在按流程模式中保留。
- DEC-0036-01A 的“仅绑定流程才校验字段权限、按钮事件和签名字段；清除流程即清理绑定配置”由 DEC-0067 局部替代。表单模板版本必选、流程版本冻结、受控升级及使用情况分析不被替代。
- DEC-0073 的当前节点动作投影和服务端重验边界保留；本次不修改该工作树决策。
- DEC-0058 的独立字段签名与按钮前签署不是同一入口，本次不重定义其认证、失效或历史证据契约。

## original-evidence

以下是建模开始时直接读取的工作树源码边界，可能随后被主智能体修改；不是本次闭环实现证据，也不是测试通过证明。

- `WorkTemplateEditor.tsx` 的表单节点配置以可选 `formProcessVersionId` 为入口，清除流程时清空 `fieldPermissions` 和 `eventBindings`，权限区域依赖 `selectedProcessOption`。
- `ProductProcessVersionEditorDialog.tsx` 的 `forms` 草稿和保存映射当时只有目录项、表单版本、必填与顺序等字段；`ProductProcessOperationFormBinding.java` 当时没有本次填报设置字段。
- `ExecutionSnapshotBuilder.java` 当时对工序表单读取版本、目录项及 required；作业表单复制 binding，存在 `formProcessVersionId` 时装入已发布流程图。这只能证明现有快照接入位置。

## inference

以下来自交接包的工程方案，不进入权威业务规则，不写成用户确认：

- 已撤销的历史推断：缺失模式时按既有流程引用推导，原无流程保持开放默认填报，并以在途快照不回写维持旧实例兼容。仅保留此记录用于追溯，不再实施或验收；新执行快照不可变另由最新 user-confirmed 指令保留。
- 两种填报模式的草稿配置独立保留，只让选中模式生效；模式切换不丢失配置。运行必须遵守所选模式是确认范围，非活动草稿如何保留是工程选择，与已取消的新旧数据双轨兼容无关。
- 复用 START 规则表示直接填报；工序表单绑定新增 nullable JSON 文本字段；复用既有保存和运行审计。
- 保存/发布拒绝无效配置以及新模式沿用稳定 fieldId 是交接包的实现方案；按流程模式既有 fieldId 契约仍有效。

## L2 影响与待验收证据

- 直接影响：作业 FORM 节点与产品/产品簇工序绑定的模式和配置；前端共用组件，workflow 负责共享配置与校验，masterdata 负责持久化和版本复制。
- 传递影响：production 负责冻结配置、运行权限、按钮和签署消费；个人表单待办与生产动作必须一致。
- 潜在影响：无额外已确认范围；模式存储与流程引用混用可能让非活动配置泄漏到运行，这是待测风险而非已发生缺陷。
- 不受影响：独立表单变更/作废审批、DHR 汇总审核策略、独立字段签名规则、自定义挂载表单的配置入口不在本次扩展范围。
- 兼容与迁移：user-confirmed；当前 MVP 旧数据可重造，取消历史回填、新旧双轨兼容、旧模式推导和旧数据自动迁移，不再要求历史已发布配置或在途旧实例保持原行为。取消的是旧数据兼容方案，不据此取消新结构所需的建表/加列等工程工作；具体实现仍由主智能体核对。
- 快照：验证保存重读、发布冻结、新建实例消费、模式隔离与新执行快照不可变，不以编辑器展示证明运行闭环。
- 审计：验证配置前后快照和运行动作审计确实包含适用设置；复用既有审计不等于证据已齐全。
- 权限：验证命中/未命中主体、默认只读与字段例外、直接调用服务端动作、按钮签署阻断和签名字段绑定；保留来源资格及当前节点重验。
- 测试：两入口的模式选择、直接提交完成、流程审批回归、配置保存重读、版本复制、新执行快照不可变、签署与权限拒绝仍需实现与测试证据并经独立质量收尾。历史兼容不再是验收要求；改为核对未保留旧数据兼容分支、回填或自动迁移方案。
- 扩展路径：configuration + product-core。配置属于各绑定，共享组件/校验与既有执行引擎属于产品内核，不增加客户条件分支。

本次不登记新的已验证执行契约，不宣称浏览器、数据库迁移、运行测试或发布验证通过。P0 保持 in-progress，P1/P2/P3 暂缓。
