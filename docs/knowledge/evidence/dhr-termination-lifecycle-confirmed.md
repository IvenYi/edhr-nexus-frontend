# DHR 提前结束生命周期确认与证据边界

知识基线：`0.3.24`；日期：2026-09-23；决策包：`DEC-PACKAGE-20260923-DHR-TERMINATION-IMPLEMENT`；级别：L2。现行增量成熟度为 `specified/internal`，不代表运行能力已验收。

## user-confirmed

本次用户消息明确将 `docs/dhr-termination-lifecycle-design.md` 的推荐本期政策视为已确认：生产对象提前结束同步使关联 DHR 真实终止；保留真实已保存记录；停止普通执行；不自动作废表单；不新增独立质量审批。终止后补录与异常结案审核延期。企业 SOP 的额外签名要求只作为未来配置研究，不阻塞本期且不据此增加审批。

上述确认解决了设计文档标题和第 9 节撰写时的“待确认”；设计文档的实现建议不自动变成确定的数据库或接口结构。`DEC-0069` 只对本期政策做权威增量建模。

## secondary-reference 与历史边界

- `DEC-0018` 已规定工单/生产对象 EARLY_TERMINATED 与普通执行停止，其旧生产规则的 `abandon-dhr` 可被误读为作废；本次将其明确改为 DHR 真实终止、表单保留。工单级直接命令仍非本期交付。
- `DEC-0062` 的唯一 DHR、首次开工和正常完工同步保留；其“不定义提前结束”和作废排除只代表当时切片范围，由 `DEC-0069` 局部替代。
- `DEC-0063` 的三类候选、不可变汇总和冻结审核策略，以及 `DEC-0068` 的正常完工补录、人工退回和新版本历史仍适用；不得据此自动开放终止后补录。
- 设计文档第 7 节的 L2 影响范围和第 8 节的验收条件由主开发实施、独立质量验证。本角色未执行运行时验收。

## original-evidence 与 inference

2026-09-23 静态读取当前工作树：`ProductionService.endObject` 正调用 `DhrInstanceService.terminateWithProductionObject`；后者已有 DHR 状态写入及终止快照逻辑。同次读取中 `DhrInstanceService.list` 的状态参数仍仅接受 `IN_PROGRESS/COMPLETED`，说明并行实现未稳定。本观察只为主开发定位，不提升任何新规则到 `implemented` 或 `verified`。原始源码和设计中已核对的冠骋源码结论分别归为本项目原始观察与设计文档的二手参考；不能把冠骋的表单作废策略引入本产品。

具体 SQL 迁移、接口字段、统一状态投影、锁顺序、不可变留存结构和旧数据修正脚本均为实施路径；须由主开发以最终源码、迁移和测试核对。尤其不能用当前可变引用伪装终止时快照，也不能把工作树代码视为发布版本。

## L2 impactAnalysis 与 extensionStrategy

直接：DHR 真实状态、一次生产终止事务、只读终止视图和允许动作。传递：生产执行、通用表单/工作流入口、待办、汇总/审核候选、状态查询计数和共享实例。潜在：终止与保存/完工/审核并发、记录控制生效后的历史读取及任务投影，依最终实现核对。不受影响：首次开工唯一实例、正常完工、既有 DHR_SUMMARY 冻结绑定、不可变正式汇总、三类来源、已完成其他对象及产品放行。兼容迁移：只修正无汇总草稿/版本的明确历史错位，冲突人工核对，缺失历史证据标缺失。快照：保留终止时服务端已保存内容与目录，不把后续受控变化回写。审计：记录原状态、原因、操作者、服务端时间和关联动作。权限：查看、终止、记录控制动作各自授权；额外签名待未来 SOP 研究。测试：事务、并发、幂等、迁移、权限、防绕过、共享实例、状态投影和正常路径回归。当前证据缺口是最终迁移、跨入口阻断、终止留存和测试报告。

`extensionStrategy.selectedPaths: [product-core, transaction-orchestration]`。生产模块拥有终止命令与生产状态；DHR 域拥有生命周期、留存和统一投影；表单/工作流域拥有实例与待办的可执行性。记录控制资格仍归其自身模块。该所有权边界不预设具体接口或数据表。

本期不修改运行代码，不执行真实数据库迁移、浏览器或发布级验证。知识模型维持 0.3.24 工作树基线；正式知识测试在本次文件更新后单独执行。
