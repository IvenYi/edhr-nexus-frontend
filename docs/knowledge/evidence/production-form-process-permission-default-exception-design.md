# 表单流程字段权限默认值与例外字段设计

本记录对应表单流程配置切片中已确认的配置投影设计，不代表生产运行时已实现。

- 填报权限组和审批节点参与者均采用“默认权限 + 例外字段”的表达方式。默认权限只有“可编辑”和“只读”两种，例外字段引用流程字段。
- 流程字段是跨多个表单复用时的稳定业务含义；作业流程表单填报节点绑定具体表单时，将流程字段映射到该表单的实际字段。
- 绑定阶段只维护字段映射，不重新定义流程中的审批权限；运行时权限解析、审批任务和实例快照仍属于后续能力。
