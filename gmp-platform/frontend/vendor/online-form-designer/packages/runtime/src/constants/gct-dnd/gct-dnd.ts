/**
 * 拖拽放置时的插入位置
 *
 * @export
 * @enum {number}
 */
export enum GCT_DND_INSERT_POS {
  /**
   * 插入第一个
   */
  FIRST = 'first',
  /**
   * 插入前
   */
  BEFORE = 'before',
  /**
   * 插入后
   */
  AFTER = 'after',
  /**
   * 插入最后
   */
  LAST = 'last',
}
