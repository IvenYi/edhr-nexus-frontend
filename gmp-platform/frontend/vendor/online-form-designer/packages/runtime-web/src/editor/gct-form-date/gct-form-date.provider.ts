import { IEditorProvider } from '@gct/runtime';

/**
 * 文本编辑器
 *
 * @export
 * @class GctFormDateProvider
 * @implements {IEditorProvider}
 */
export class GctFormDateProvider implements IEditorProvider {
  component = 'gct-form-date';
}
