import { ITableEditorProvider } from '@gct/runtime';

/**
 * 文本编辑器
 *
 * @export
 * @class GctTableDateProvider
 * @implements {ITableEditorProvider}
 */
export class GctTableDateProvider implements ITableEditorProvider {
  component = 'gct-table-date';
}
