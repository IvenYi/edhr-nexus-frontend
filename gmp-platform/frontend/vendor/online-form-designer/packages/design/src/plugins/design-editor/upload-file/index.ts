import { FIELD_TYPE } from '@gct/runtime';
import { UploadFile } from './upload-file';
import { widthDesignEditorInstall } from '../../../utils';

export default widthDesignEditorInstall(FIELD_TYPE.ATTACHMENT, UploadFile);
