import { FIELD_TYPE } from '@gct/runtime';
import { UploadImage } from './upload-image';
import { widthDesignEditorInstall } from '../../../utils';

export default widthDesignEditorInstall(FIELD_TYPE.IMAGE, UploadImage);
