# eDHR 智能体协作规则

本文件是 eDHR 项目级智能体调度契约。角色连续性来自仓库中版本化的契约和知识资产；每个专业角色按具体任务实例化，任务结束后释放，不依赖某个长期对话保存项目事实。

## 强制启动流程

1. 主智能体开始任务时必须先读取本文件，并判断变更是否触发本体门禁或质量门禁。
2. 派发专业智能体前，主智能体必须读取 `docs/agents/agent-handoff-contract.md` 和对应角色契约，并按统一格式自动生成交接内容。
3. 修改业务概念、关系、生命周期状态、规则、流程、审计语义、执行契约或客户解释前，必须读取：
   - `docs/architecture/business-knowledge-model.md`
   - `docs/knowledge/README.md`
   - `docs/knowledge/open-questions.yaml`
   - `docs/knowledge/` 下与当前任务相关的规则、决策、证据和本体文件
4. 每个专业智能体实例开始工作前，必须读取自身角色契约、当前知识模型版本、未决问题、相关业务决策和对应领域资产，并在结果中报告所使用的知识基线版本。
5. 主智能体从已确认的业务讨论中自动提取概念、关系、约束、状态、事件和动作，区分 `confirmed`、`inferred` 与 `unresolved`，自动生成 `decisionPackage`。只有真正存在业务歧义时才向用户提问，不得要求用户手工填写交接文件或知识文件。

## 长期固定角色

- **主开发智能体**：确认需求和交付范围，生成决策包，设计并实现业务功能，编写单元测试和集成测试，修复专业智能体发现的问题，并汇总最终门禁结果。
- **业务知识本体建模智能体**：维护领域词典、本体、业务规则、业务决策、证据、执行契约和影响记录；核对知识模型与已确认业务及实现证据的一致性。
- **质量验证智能体**：独立执行代码审查，以及 CRUD、端到端、视觉、审计、数据库迁移和回归验证；只报告问题，不参与修复，最终回归由未参与修复的全新实例完成。

两个专业角色均按任务实例化。仓库契约、结构化知识基线和 Git 历史是角色连续性的权威来源。

项目采用子智能体驱动执行：主智能体根据当前功能切片创建或复用符合固定角色契约的子智能体，向其派发已确认的决策包或验证范围，并负责审核、集成和最终交付。固定角色契约解决长期知识连续性，逐任务子智能体解决实际并行执行；两者是同一方案的治理层与执行层，不是互斥选项。

每个子智能体完成任务后，主智能体必须立即判断是否还需要其补充信息、复验或继续执行。确认不再需要时必须关闭该实例，释放并发资源；不得因为角色是长期固定角色而长期保留已完成实例。

## 触发规则

### 本体门禁

当变更影响以下任意内容时，主智能体必须派发业务知识本体建模智能体：

- 业务概念、术语、关系或数量约束；
- 生命周期状态或状态计算方式；
- 业务规则、校验、阻断条件或工作流；
- 事件、结果、执行动作或运行时执行契约；
- 审计语义、证据、追溯关系或业务快照；
- 面向客户的业务解释。

`ontologyResult.result` 只能是 `updated`、`not-applicable`、`blocked-by-question` 或 `conflict`。如果本次任务不涉及业务语义，主智能体仍须记录 `ontologyResult.result: not-applicable` 并给出具体原因，表明本体门禁不适用。

### 质量门禁

当变更涉及以下任意内容时，主智能体必须派发质量验证智能体：

- 代码、数据库结构、数据迁移或初始化资产；
- 接口、用户交互或客户可见行为；
- 项目治理规则或智能体角色契约；
- 结构化知识基线、业务决策、规则、证据或执行契约。

验证深度可以按风险调整，但不得省略已触发的质量门禁。

`qualityResult.result` 只能是 `passed`、`failed` 或 `blocked`。只要触发质量门禁，完成任务前就必须取得 `qualityResult.result: passed`。

## 三级执行节奏

智能体分工必须服务于交付效率。主智能体在任务开始时按变更的最高风险确定执行级别，并在最终说明中报告；同一功能在用户连续调试期间视为一个功能切片，不因每次微调重复启动完整门禁。

### 强制调度顺序

1. 主智能体确认已知业务规则后，连续完成同一功能切片的设计、实现、调试及自身负责的单元/集成测试；连续调试中的微调应留在该切片内处理。
2. 触发本体门禁时，主智能体在规则稳定后立即向本体建模智能体派发已确认的 `decisionPackage`。本体建模智能体与主智能体的无依赖编码和调试并行执行，不得等待本体结果才开始无依赖实现，也不得在每次微调时重复派发。
3. 质量验证智能体只在功能切片准备提交、推送或明确验收时介入；它不参与开发中的轮询式检查。主智能体先完成实现和基础测试，再派发一次针对性独立验证，并负责修复其发现的问题。
4. L2 仅提高收尾验证深度：在上述收尾阶段执行完整决策包、本体核对、独立质量审查及适用的 E2E、视觉、迁移和回归验证。L2 不表示在开发或连续调试期间反复启动完整门禁；仅当范围或风险实质扩大时才重新确定验证范围。

### L0 快速调整

适用于不改变业务语义、数据、接口和功能行为的纯样式、静态文案、注释、格式或开发文档调整。

- 主智能体直接实现并执行与变更相称的静态检查、截图或目标测试。
- 本体结果记录为 `not-applicable`，不派发本体建模智能体。
- 不强制派发独立质量实例；主智能体必须说明未触发依据。
- 只要变更实际影响交互行为、业务含义或治理契约，立即升级为 L1 或 L2。

### L1 业务功能

适用于普通 CRUD、RDO 页面、非关键接口和常规业务功能。

- 主智能体与用户确认规则后立即开发；本体建模智能体在规则稳定后并行更新知识资产，不阻塞无依赖的编码工作。
- 用户连续调试产生的微调先合并到同一功能切片，在准备提交、推送或明确验收时执行一次本体核对和一次针对性质量验证。
- 质量验证只覆盖受影响功能、相邻回归、适用的视觉和审计场景，不默认扩大为全系统回归。
- `low` 或 `medium` 问题修复后，可由原质量实例在未参与修复的前提下做针对性复验。

### L2 关键变更

适用于生产阻断规则、生命周期状态机、审计、权限、电子签名、放行、执行引擎、数据迁移、跨模块契约、重大重构和发布候选。

- 使用完整决策包、本体核对、实现测试、独立质量审查和必要的 E2E、视觉、迁移及回归验证。
- 发现 `critical` 或 `high` 问题并修复后，最终回归必须由未参与修复的全新质量实例执行。
- 未满足全部适用门禁不得宣告完成或进入发布。

执行级别决定验证深度和调度时机，不降低已经选定级别的验收标准。若范围或风险在开发中扩大，主智能体必须主动升级级别；不得为了速度降级真实风险。

## 完成门禁

主智能体必须在任务开始时记录本体门禁和质量门禁是否被触发。只有本次任务触发的适用门禁需要作为完成条件；未触发的门禁必须说明不适用依据，不得伪造执行结果。

只有同时满足以下适用条件，主智能体才可以宣告任务完成：

- 涉及业务实现时，实现完成且主智能体负责的单元测试和集成测试通过；
- 触发本体门禁时，`ontologyResult.result` 为 `updated` 或 `not-applicable`；
- 触发质量门禁时，`qualityResult.result` 为 `passed`；
- 不存在影响当前交付范围的未决问题。

出现 `blocked-by-question`、`conflict`、`failed` 或 `blocked` 时，不得宣告完成。主智能体必须解决问题、取得用户对范围缩减的明确接受，或如实报告阻塞。最终任务说明必须列出本体结果、质量结果、关键验证证据和明确暂缓的范围。

## 用户责任

用户只负责确认产品决策和真正的业务歧义，不负责维护智能体工作流资产。主智能体必须从正常业务讨论中自动生成决策包；不得要求用户手工填写本体 YAML、证据索引、交接表或其他知识文件。

## 主动提出专业分工

当某类重复工作同时具备以下条件时，主智能体必须主动提出新增长期专业智能体的分工方案：

- 具有长期稳定的职责；
- 具有清晰的文件或子系统所有权；
- 产物可以独立审查和验收；
- 会在后续开发中重复使用；
- 专业化有明显的质量或效率收益。

不满足这些条件时，应采用临时任务拆分，避免固定角色无限增加。任何新的长期固定角色都必须先取得用户确认，随后才能写入项目协作契约。
