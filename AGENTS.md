# eDHR 智能体协作规则

本文件是 eDHR 项目级智能体调度契约。角色连续性来自仓库中版本化的契约和知识资产；每个专业角色按具体任务实例化，任务结束后释放，不依赖某个长期对话保存项目事实。

`codeplzreadme.md` 是本项目的强制编码行为准则，与本文件配套使用，不是可选参考。`AGENTS.md` 负责项目治理、业务门禁和智能体协作；`codeplzreadme.md` 是开发代码行为细则的唯一详细来源，负责编码前思考、修改范围、简化原则和风险匹配验证。主开发智能体以及任何实际修改代码、测试、迁移或配置的智能体必须遵守它；只做本体建模或质量审查的智能体以各自角色契约为准，只吸收其中适用于自身工作的通用原则。两者出现冲突时，暂停相关工作并明确冲突，不能自行静默取舍。

## 强制启动流程

1. 主智能体以及任何实际修改代码、测试、迁移或配置的智能体开始相关工作前，必须先读取本文件和 `codeplzreadme.md`；本体和质量智能体按各自角色契约开始工作，并判断变更是否触发本体门禁或质量门禁。
2. 开始编码前，主智能体必须明确当前需求理解、已知假设、真实未决问题、最小修改范围、成功标准、执行级别和验证方式；存在会改变方案或验收结果的歧义时，必须先与用户澄清，不得擅自补全。
3. 派发专业智能体前，主智能体必须读取 `docs/agents/agent-handoff-contract.md` 和对应角色契约，并按统一格式自动生成交接内容。
4. 修改业务概念、关系、生命周期状态、规则、流程、审计语义、执行契约或客户解释前，必须读取：
   - `docs/architecture/business-knowledge-model.md`
   - `docs/knowledge/README.md`
   - `docs/knowledge/open-questions.yaml`
   - `docs/knowledge/` 下与当前任务相关的规则、决策、证据和本体文件
5. 每个专业智能体实例开始工作前，必须读取自身角色契约、当前知识模型版本、未决问题、相关业务决策和对应领域资产，并在结果中报告所使用的知识基线版本。
6. 主智能体从已确认的业务讨论中自动提取概念、关系、约束、状态、事件和动作，区分 `confirmed`、`inferred` 与 `unresolved`，自动生成 `decisionPackage`。只有真正存在业务歧义时才向用户提问，不得要求用户手工填写交接文件或知识文件。

### 编码行为准则的执行门槛

以下门槛仅适用于主开发智能体以及实际修改代码、测试、迁移或配置的实现智能体；本体和质量智能体继续以各自角色契约为准。

- **先思考再编码**：说明假设和方案选择；存在多种合理解释时列出取舍；无法安全判断时先提问。
- **多步骤先计划**：对涉及多个文件、多个验证环节或可能中断的任务，先列出简短步骤和每步验证方式；执行中按实际进展更新，不把计划当成完成证明。
- **保持简单**：只实现已确认范围，不为未来猜测增加抽象、配置项或额外实现；能用局部改动解决时不做无关重构。
- **精准修改**：每一处改动都必须能追溯到需求、缺陷修复、测试或必要的实现配套；保留用户已有改动，不覆盖、回滚或清理无关代码。
- **代码卫生**：手工编辑使用 `apply_patch`；只清理本次改动造成的无用导入、变量和函数；不得使用破坏性 Git 操作。
- **目标驱动验证**：实现智能体为每项实现定义可检查的成功标准，并在验证失败时继续定位和修复，不能用“已修改”代替“已验证”。质量智能体只报告问题，不参与修复。
- **风险匹配验证**：纯展示调整执行静态检查或目标构建即可；逻辑、数据、接口、持久化、流程、审计和权限变更执行对应的聚焦测试；高风险用户流程在条件允许时验证真实交互。
- **诚实报告**：实现智能体最终说明实际修改、实际运行的检查、未执行的验证、残余风险和明确暂缓范围；质量智能体按角色契约报告独立验证结果，不得声称未执行的浏览器、数据库或质量验证已经通过。

### 原始证据优先

涉及外部产品、竞品或历史系统的调研，已有调研记录、摘要、截图说明和知识库证据只能作为索引、线索和补充参考，不得作为本次调研的唯一事实来源。主智能体必须从可获得的原始资料重新核对：

- 源码问题优先查看原始前端、后端、数据库和接口代码；
- 页面行为优先查看可运行页面、原始录屏或原始截图；
- 数据结构优先查看原始迁移、实体、接口模型和真实样例；
- 已有调研文档中的结论必须标注为二手线索，除非回到原始证据复核，否则不得写成已确认事实；
- 原始资料不可获得时，必须明确记录证据缺口和不确定性，不得用调研记录中的推断补足缺失证据。

调研记录可以帮助定位原始证据，但不能替代原始证据。最终报告必须区分 `original-evidence`、`secondary-reference`、`user-confirmed` 和 `inference`。

## 需求影响分析与扩展路径

需求影响分析属于主开发智能体的设计职责，不新增长期专业角色，也不新增独立审批流程。主智能体使用本体、规则、历史决策、实现证据和源码核对需求影响；本体建模智能体在规则稳定后同步结构化知识和证据链接，质量验证智能体只在收尾阶段核验影响范围与实现是否一致。

影响分析必须服从前期产品高速迭代目标：

- 开始实现前只完成足以识别业务边界、主要风险和扩展路径的最小分析，不等待穷举全部依赖后才开始无依赖编码。
- 同一功能切片在连续讨论、视觉调整和业务微调期间只维护一份 `impactAnalysis`，增量更新，不重复生成决策包或反复派发专业智能体。
- 已有知识和证据足以支持判断时直接复用，不为形式完整重复搜索、重复建模或重复验证。
- 证据不足但不影响当前实现的内容写入 `potentialImpacts` 或 `evidenceGaps`；只对可能改变方案、数据兼容、生产安全或验收结果的缺口深入查证。
- 默认将分析保存在当前任务的 `decisionPackage` 和交接上下文中；只有跨模块 L2 变更、长期架构决策或需要团队复用时才沉淀独立影响报告。
- 用户只确认真实业务歧义。影响项、证据链接和扩展路径由主智能体自动总结，不要求用户维护分析表格。

`extensionStrategy` 必须从以下路径中选择一种或多种，并说明选择依据和代码所有权边界：`configuration`、`transaction-orchestration`、`standard-action`、`project-plugin`、`product-core`。可以组合使用，但不得把可配置差异散落成页面、控制器或监听器中的客户条件分支。

分析深度按执行级别控制：

- **L0**：不生成正式 `impactAnalysis`，只记录为什么不影响业务语义、数据、接口和功能行为。
- **L1**：使用精简摘要，至少列出直接影响、主要相邻风险、主要实现证据、测试范围和扩展路径；没有实际内容的分类保持空列表，不为填满字段扩大调研。
- **L2**：覆盖直接、传递、潜在、不受影响、兼容与迁移、生产快照、审计、权限和测试范围，并明确证据缺口及处置。

影响分析反映当前最佳证据，不是自动完整的代码依赖图。Codex 必须查证容易漂移且会影响交付判断的源码、接口、数据库和测试；无法确认的内容必须标注不确定性。

## 长期固定角色

- **主开发智能体**：确认需求和交付范围，生成决策包和影响分析，选择可维护的扩展路径，设计并实现业务功能，编写单元测试和集成测试，修复专业智能体发现的问题，并汇总最终门禁结果。
- **业务知识本体建模智能体**：维护领域词典、本体、业务规则、业务决策、证据、执行契约和影响记录；核对知识模型与已确认业务及实现证据的一致性。
- **质量验证智能体**：独立执行代码审查，以及 CRUD、端到端、视觉、审计、数据库迁移和回归验证；只报告问题，不参与修复，最终回归由未参与修复的全新实例完成。

两个专业角色均按任务实例化。仓库契约、结构化知识基线和 Git 历史是角色连续性的权威来源。

项目采用子智能体驱动执行：主智能体根据当前功能切片创建或复用符合固定角色契约的子智能体，向其派发已确认的决策包或验证范围，并负责审核、集成和最终交付。固定角色契约解决长期知识连续性，逐任务子智能体解决实际并行执行；两者是同一方案的治理层与执行层，不是互斥选项。

每个子智能体完成任务后，主智能体必须立即判断是否还需要其补充信息、复验或继续执行。确认不再需要时必须关闭该实例，释放并发资源；不得因为角色是长期固定角色而长期保留已完成实例。

## 主智能体与专业子智能体的运行边界

一个独立功能上下文本身就是该功能的主智能体，负责需求澄清、影响分析、方案设计、实现、调试、集成和最终交付。不得为同一功能再创建一个“主开发子任务”来承接主要开发工作，也不得通过一个上下文向另一个独立上下文发送消息来模拟主子智能体关系。

本体建模和质量验证属于主智能体按门禁临时派发的专业子智能体。专业子智能体只承担明确交接范围内的本体维护或独立验证，并将结果返回主智能体；主智能体负责审核、集成、修复问题和最终验收。专业子智能体完成后应立即释放。

用户可见的独立 task/thread 只用于用户明确要求的新功能上下文或独立任务，不能默认视为专业子智能体。项目契约可以约束职责、权限、文件所有权和完成门禁，但不能强制 Codex 客户端以右侧树状面板显示子智能体。只有平台提供真实的 subagent/multi-agent 调度能力时，才能将本体或质量工作派发为专业子智能体；若当前环境不提供该能力，主智能体不得用平级开发线程替代，应自行完成工作或明确报告门禁受限。

## 触发规则

### 本体门禁

当变更影响以下任意内容时，主智能体必须派发业务知识本体建模智能体：

- 业务概念、术语、关系或数量约束；
- 生命周期状态或状态计算方式；
- 业务规则、校验、阻断条件或工作流；
- 事件、结果、执行动作或运行时执行契约；
- 审计语义、证据、追溯关系或业务快照；
- 面向客户的业务解释。

`ontologyResult.result` 只能是 `updated`、`not-applicable`、`blocked-by-question` 或 `conflict`。如果本次任务不涉及业务语义，主智能体仍须记录 `ontologyResult.result: not-applicable` 并给出具体原因，表明本体门禁不适用。

### 质量门禁

当变更涉及以下任意内容时，主智能体必须派发质量验证智能体：

- 代码、数据库结构、数据迁移或初始化资产；
- 接口、用户交互或客户可见行为；
- 项目治理规则或智能体角色契约；
- 结构化知识基线、业务决策、规则、证据或执行契约。

验证深度可以按风险调整，但不得省略已触发的质量门禁。

`qualityResult.result` 只能是 `passed`、`failed` 或 `blocked`。只要触发质量门禁，完成任务前就必须取得 `qualityResult.result: passed`。

## 三级执行节奏

智能体分工必须服务于交付效率。主智能体在任务开始时按变更的最高风险确定执行级别，并在最终说明中报告；同一功能在用户连续调试期间视为一个功能切片，不因每次微调重复启动完整门禁。

### 强制调度顺序

1. 主智能体确认已知业务规则并完成与执行级别相称的最小影响分析后，连续完成同一功能切片的设计、实现、调试及自身负责的单元/集成测试；最小影响分析不得阻塞无依赖编码，连续调试中的微调应留在该切片内处理。
2. 触发本体门禁时，主智能体在规则稳定后立即向本体建模智能体派发已确认的 `decisionPackage`。本体建模智能体与主智能体的无依赖编码和调试并行执行，不得等待本体结果才开始无依赖实现，也不得在每次微调时重复派发。
3. 质量验证智能体只在功能切片准备提交、推送或明确验收时介入；它不参与开发中的轮询式检查。主智能体先完成实现和基础测试，再派发一次针对性独立验证，并负责修复其发现的问题。
4. L2 仅提高收尾验证深度：在上述收尾阶段执行完整决策包、本体核对、独立质量审查及适用的 E2E、视觉、迁移和回归验证。L2 不表示在开发或连续调试期间反复启动完整门禁；仅当范围或风险实质扩大时才重新确定验证范围。

### L0 快速调整

适用于不改变业务语义、数据、接口和功能行为的纯样式、静态文案、注释、格式或开发文档调整。

- 主智能体直接实现并执行与变更相称的静态检查、截图或目标测试。
- 本体结果记录为 `not-applicable`，不派发本体建模智能体。
- 不强制派发独立质量实例；主智能体必须说明未触发依据。
- 只要变更实际影响交互行为、业务含义或治理契约，立即升级为 L1 或 L2。

### L1 业务功能

适用于普通 CRUD、RDO 页面、非关键接口和常规业务功能。

- 主智能体与用户确认规则后立即开发；本体建模智能体在规则稳定后并行更新知识资产，不阻塞无依赖的编码工作。
- 主智能体在同一决策包中维护精简 `impactAnalysis` 和 `extensionStrategy`，只覆盖当前功能、主要相邻风险和必要证据，不默认生成独立报告。
- 用户连续调试产生的微调先合并到同一功能切片，在准备提交、推送或明确验收时执行一次本体核对和一次针对性质量验证。
- 质量验证只覆盖受影响功能、相邻回归、适用的视觉和审计场景，不默认扩大为全系统回归。
- `low` 或 `medium` 问题修复后，可由原质量实例在未参与修复的前提下做针对性复验。

### L2 关键变更

适用于生产阻断规则、生命周期状态机、审计、权限、电子签名、放行、执行引擎、数据迁移、跨模块契约、重大重构和发布候选。

- 使用完整决策包和影响分析、本体核对、实现测试、独立质量审查和必要的 E2E、视觉、迁移及回归验证。
- 发现 `critical` 或 `high` 问题并修复后，最终回归必须由未参与修复的全新质量实例执行。
- 未满足全部适用门禁不得宣告完成或进入发布。

执行级别决定验证深度和调度时机，不降低已经选定级别的验收标准。若范围或风险在开发中扩大，主智能体必须主动升级级别；不得为了速度降级真实风险。

## 完成门禁

主智能体必须在任务开始时记录本体门禁和质量门禁是否被触发。只有本次任务触发的适用门禁需要作为完成条件；未触发的门禁必须说明不适用依据，不得伪造执行结果。

只有同时满足以下适用条件，主智能体才可以宣告任务完成：

- 涉及业务实现时，实现完成且主智能体负责的单元测试和集成测试通过；
- L1、L2 业务实现已具有与风险相称的 `impactAnalysis` 和明确的 `extensionStrategy`；
- 触发本体门禁时，`ontologyResult.result` 为 `updated` 或 `not-applicable`；
- 触发质量门禁时，`qualityResult.result` 为 `passed`；
- 不存在影响当前交付范围的未决问题。

出现 `blocked-by-question`、`conflict`、`failed` 或 `blocked` 时，不得宣告完成。主智能体必须解决问题、取得用户对范围缩减的明确接受，或如实报告阻塞。最终任务说明必须列出本体结果、质量结果、关键验证证据和明确暂缓的范围。

## 用户责任

用户只负责确认产品决策和真正的业务歧义，不负责维护智能体工作流资产。主智能体必须从正常业务讨论中自动生成决策包；不得要求用户手工填写本体 YAML、证据索引、交接表或其他知识文件。

## 主动提出专业分工

当某类重复工作同时具备以下条件时，主智能体必须主动提出新增长期专业智能体的分工方案：

- 具有长期稳定的职责；
- 具有清晰的文件或子系统所有权；
- 产物可以独立审查和验收；
- 会在后续开发中重复使用；
- 专业化有明显的质量或效率收益。

不满足这些条件时，应采用临时任务拆分，避免固定角色无限增加。任何新的长期固定角色都必须先取得用户确认，随后才能写入项目协作契约。
